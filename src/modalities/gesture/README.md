# Gesture Adapter

This module should encapsulate all gesture-specific perception code.

Expected responsibilities:

- camera capture
- landmark detection
- gesture classification
- position estimation
- emission of normalized `GestureEvent` objects
