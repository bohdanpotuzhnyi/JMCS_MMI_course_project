# Action Schema

This module defines app-independent canonical actions emitted by the collaboration core.

The rest of the system should depend on these actions instead of raw speech or gesture events.
