# Full runtime dependencies for the multimodal toolkit.
#
# Install with:
#   pip install -r requirements.txt
#
# Notes:
# - The voice modality also needs a local Vosk model directory.
#   Set VOSK_MODEL_PATH or place a model under:
#   src/modalities/voice/models/vosk-model
# - PyAudio may require the PortAudio system library on some platforms.

# Shared contracts and data models
pydantic>=2.7,<3

# Gesture modality
numpy>=1.26,<3
opencv-python>=4.10.0
mediapipe>=0.10.14
Pillow>=10.4.0

# Voice modality
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
vosk>=0.3.44,<0.4
